"""
main.py
-------
Entry point for the LL(1) Predictive Parser Demonstration application.

Run with:
    python3 main.py

Requires only the Python standard library (Tkinter ships with the
standard CPython distribution on Windows/macOS; on Linux you may need
to install it separately, e.g. `sudo apt install python3-tk`).
"""

from gui import launch

if __name__ == "__main__":
    launch()
